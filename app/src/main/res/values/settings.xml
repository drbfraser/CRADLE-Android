<?xml version="1.0" encoding="utf-8"?>

<!-- Default Preference Values -->

<resources>
    <!-- Network -->
    <string name="settings_default_server_hostname">cradleplatform.com</string>
    <string name="settings_default_server_port" />
    <string name="settings_default_server_use_https">true</string>

    <!-- OCR -->
    <string name="settings_default_ocr_enabled">true</string>
    <string name="settings_default_ocr_show_debug_info">false</string>

    <!-- Sync -->
    <string name="settings_periodic_sync_enabled">true</string>

    <!-- Notification Channel -->
    <string name="notification_id">iCradle</string>

    <!-- Recheck Duration -->
    <integer name="recheck_duration">15</integer>

    <!-- Encouraged Sync Period (user will be reminded to manually sync) -->
    <integer name="settings_default_sync_period_hours">3</integer>

    <!-- Periodic Sync Period (app will automatically sync, if user enabled periodic sync) -->
    <integer name="settings_periodic_sync_hours">24</integer>

    <!-- Pin Activity Prefs -->
    <string name="key_pin_shared_pref">PIN_SHARED_PREF</string>
    <string name="key_pin_shared_key">PIN_CODE_KEY</string>
    <string name="key_pin_default_pin">aaaa</string>

    <!-- Default Relay Phone Number -->
    <string name="settings_default_relay_phone_number">+15555215554</string>

</resources>
